# v0.5 Completion Audit

This audit covers the v0.5 release candidate. It is intentionally bounded:
the candidate establishes reproducible simulation, replay, calibration, and
training evidence handling. It does not establish a real robot's safety,
performance, or authorization to move.

| Exit gate | Evidence | Status |
|---|---|---|
| Closed simulation admission cannot promote hardware | Admission, training, and replay tests retain `hardware_promotable: false`; all claim-driving parts remain placeholders | MET |
| Source-bound ROS workspace | Canonical manifest receipt `c228d2a1b0b4fe774bde1f114a494a6c64fb8923cf86a71c1e24d3006caa80af`; stale/tampered files are rejected | MET |
| Bounded scenarios and replay | Portable benchmark compiles and replays 10/10 canonical scenarios; trace receipts are revalidated instead of trusted | MET |
| Independent dynamics and calibration boundary | Two distinct calculations agree within declared intervals; only synthetic calibration evidence is admitted | MET |
| Training firewall | Bounded train/evaluation/held-out execution remains `simulated` and cannot raise physical evidence | MET |
| Linux simulator consumers | Jazzy/Harmonic run `31755294605` passed for `e47d6bb`; retained artifact SHA-256 `8c7acb48090911107a341238ba94d333e9497858b3fe1d64baa79e25560f7d02` records active controllers, collision-aware MoveIt readiness, and Nav2 servers | MET |
| Portable regression and distribution checks | 370 local tests, distribution validation, installer dry-run, compile check, and diff check passed; exact-candidate CI runs `31755294512` and `31755295923` passed the Linux/Windows Python matrix plus fresh installation | MET |
| Public merge, tag, release, and fresh local refresh | PR `#6` has a clean merge state and all required candidate checks passed; merge/tag/release remain the final publication actions | PENDING RELEASE |

## Claim boundary

Passing the live gate means the pinned Linux ROS 2 Jazzy, Gazebo Harmonic,
ros2_control, MoveIt, and Nav2 consumers loaded together for the reference
workspace. It does not show that a motion plan was executed safely, a scenario
was completed, a controller was tuned, a calibration reflects a physical robot,
or any component has supplier/bench/hardware evidence.

No v0.5 action authorizes purchasing, fabrication, energizing, or moving a
robot. v0.6 onward must add supplier-authenticated parts, bench measurements,
controlled integration, safety procedures, and explicit human motion authority
before any real-hardware claim can be considered.
